[pytest]
pythonpath = .
testpaths = tests
markers =
    live_marketdata: requires real Fubon marketdata credentials and ccxt network access
    readonly_broker: requires real Fubon/Binance read-only broker credentials
    dry_run_smoke: requires real read-only brokers plus live market data dry-run flow
